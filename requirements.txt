# Pinned for Python 3.9 (latest releases require >=3.10 and fail to install).
# Upgrade pip FIRST (default 20.2.3 old resolver picks wrong versions):
#   python3 -m pip install --upgrade pip
flask==3.1.3
google-auth==2.50.0
google-auth-oauthlib==1.3.1
google-api-python-client==2.198.0
protobuf<7
requests
